<Window x:Class="L9HLL.Launcher.Dialogs.AutoLaunchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Auto-Launch"
        Height="310" Width="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1a1a1a"
        Foreground="#e0e0e0"
        FontFamily="Segoe UI"
        Topmost="True">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Auto-Launching Server" FontSize="14" FontWeight="SemiBold"
                   HorizontalAlignment="Center" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" x:Name="ServerNameText" FontSize="12" Foreground="#aaa"
                   HorizontalAlignment="Center" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" x:Name="CountDownText" FontSize="48" FontWeight="Bold"
                   Foreground="#8a9a5b" HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <Button Grid.Row="3" x:Name="CancelBtn" Content="Cancel Launch" Width="120" Height="36"
                HorizontalAlignment="Center" Margin="0,12,0,0"
                Background="#8a2222" Foreground="White" BorderBrush="#8a2222"
                FontSize="13" Cursor="Hand" Click="CancelBtn_Click"/>
    </Grid>
</Window>